# ProteinTTT Configuration File

# Data settings
df_path: "/scratch/project/open-35-8/pimenol1/ProteinTTT/ProteinTTT_fresh/data/LL"  # REQUIRED: Path to your data directory
max_sequence_length: 500
use_true_pdb: false  # Set to true if you have ground truth PDB files for validation
use_chain_name: false 

# Test-Time Training settings
use_msa: false  # Set to true to use MSA (requires .a3m files in df_path/msa/)
use_gradient_clip: false
renumber_pdb: false
# gradient_clip_max_norm: 1.0
# steps: 100
# learning_rate: 1e-05
# lora_rank: 128
# lora_alpha: 256
# ags: 32 
# fgr_enabled: true
# score_seq_kind: "scaled_pseudo_perplexity"

seed: 0

# Output paths (relative to df_path)
output:
  esm_ttt_dir: "predicted_structures/ESMFold_ProteinTTT"
  esm_dir: "predicted_structures/ESMFold"
  logs_dir: "logs"
  plots_dir: "plots"
  results_file: "results.tsv"
  execution_log: "execution.log"

input:
  summary_file: "summary.csv"
  pdb_dir: ""
  msa_dir: "msa"

columns:
  id_column: "pdb_id"
  sequence_column: "sequence"
  chain_id_column: "chain_id"